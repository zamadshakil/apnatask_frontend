import { Link } from 'expo-router'; import React from 'react'; import { Text } from 'react-native'; import Button from '../../../src/components/Button'; import { Screen } from '../../../src/components/Screen'; import { useSession } from '../../../src/providers/AuthProvider'; import { Theme } from '../../../src/styles/theme';
export default function ProviderAccount(){const{user}=useSession();return <Screen><Text style={{...Theme.typography.h1}}>{user?.display_name}</Text><Text style={{color:Theme.colors.successDark,marginVertical:8}}>KYC approved provider</Text><Link href="/(customer)/(tabs)" asChild><Button title="Switch to customer" type="outline" onPress={()=>undefined}/></Link></Screen>}
